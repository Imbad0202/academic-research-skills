# ARS v3.6.5 — Material Passport `literature_corpus[]` Consumer Integration

**Status:** Design — pending implementation plan
**Author:** brainstormed 2026-04-26
**Target release:** v3.6.5 (suite invariant; patch-level)
**Predecessor:** v3.6.4 (input port shipped 2026-04-25, main `aa96fa6`)
**Repository:** Imbad0202/academic-research-skills

---

## 1. Goal & Non-Goals

### 1.1 Goal

Deliver the consumer side of the `literature_corpus[]` input port shipped in v3.6.4. When a user-written adapter has populated a Material Passport with a non-empty `literature_corpus[]`, the two Phase 1 literature agents (`deep-research/agents/bibliography_agent.md` and `academic-paper/agents/literature_strategist_agent.md`) shall enter a **corpus-first, search-fills-gap** flow and report corpus utilisation transparently in PRISMA-style search documentation.

### 1.2 Non-Goals (out of v3.6.5 scope)

- No schema change. `literature_corpus_entry.schema.json`, `rejection_log.schema.json`, and Schema 9 structural fields all remain frozen at v3.6.4 shape.
- `citation_compliance_agent` (Phase 5a) does not read corpus in v3.6.5. Deferred to v3.6.6+.
- `source_verification_agent` does not use corpus to cross-check sources in v3.6.5. Deferred to v3.6.6+.
- No new environment flag. No new mode. No mode-scoped opt-in.
- No deterministic behaviour test. v3.6.5 is prompt-layer; behaviour validation is reserved for the first real systematic-review run after ship.
- No change to `academic-paper-reviewer` suite.

### 1.3 Scope summary

- 2 agent.md files modified (`bibliography_agent` + `literature_strategist_agent`)
- 1 new reference document (`academic-pipeline/references/literature_corpus_consumers.md`)
- 1 new CI lint script (`scripts/check_corpus_consumer_protocol.py`) + unit tests
- 1 new manifest (`scripts/corpus_consumer_manifest.json`)
- `shared/handoff_schemas.md` Schema 9 paragraph: retire `Consumer-side integration deferred to v3.6.5+` caveat
- CI wired through `.github/workflows/spec-consistency.yml`
- 7-touchpoint version sweep
- Two-PR rollout under one v3.6.5 tag

---

## 2. Architecture

### 2.1 Component map

```
[ User adapter (out-of-tree) ]
        │ writes
        ▼
   passport.yaml  ──── (optional) literature_corpus[] entries
        │ schema-validated by
        ▼
   scripts/check_literature_corpus_schema.py  (existing, v3.6.4)
        │ passport flows downstream via Material Passport
        ▼
┌─────────────────────────────────────────────────────────┐
│ Phase 1 consumer agents (NEW reading behaviour, v3.6.5) │
│                                                         │
│  • deep-research/agents/bibliography_agent.md           │
│  • academic-paper/agents/literature_strategist_agent.md │
│                                                         │
│  Both agents:                                           │
│    1. Detect non-empty literature_corpus[] (presence)   │
│    2. Pre-screen against current RQ (corpus-first)      │
│    3. Honest reporting (skip / 0-hit surface)           │
│    4. Emit PRE-SCREENED block in Search Strategy Report │
│    5. Annotated bib stays neutral — no source tag       │
└─────────────────────────────────────────────────────────┘
        │ shared reading contract
        ▼
   academic-pipeline/references/literature_corpus_consumers.md  (NEW)
        │ structure enforced by
        ▼
   scripts/check_corpus_consumer_protocol.py  (NEW lint)
        │ wired into
        ▼
   .github/workflows/spec-consistency.yml (CI gate)
```

### 2.2 Architectural invariants

1. **Schema invariance.** v3.6.5 does not modify any JSON schema or Schema 9 structural field. All metadata in the PRE-SCREENED block is aggregated from existing entry fields (`obtained_via`, `obtained_at`, `adapter_name`, `adapter_version`, `citation_key`).
2. **Reading-only consumer boundary.** Consumer agents only read `literature_corpus[]`. They never modify, backfill, or derive new passport fields. Writing to the passport `literature_corpus[]` remains the sole responsibility of user adapters per v3.6.4 input port boundary.
3. **Attribution split.** Reproducibility records (which entries came from corpus, hit rate, skipped entries) live only in the Search Strategy Report's `PRE-SCREENED FROM USER CORPUS:` block. The annotated bibliography stays neutral with no source-attribution tags.

### 2.3 Cross-skill backpointer convention

Both consumer agent files carry a backpointer to `academic-pipeline/references/literature_corpus_consumers.md`. Pattern follows the v3.4.0 compliance agent's cross-skill backpointer (compliance agent in `shared/agents/` referenced from `academic-paper/references/anti_leakage_protocol.md`). Rationale: the consumer protocol is owned at the pipeline layer (mirroring v3.6.4's adapter contract location), but the actual reading behaviour lives in skill-specific agents; backpointers are the minimal coupling.

### 2.4 Out-of-scope coupling points (explicitly deferred)

- `citation_compliance_agent` Phase 5a corpus integration → v3.6.6+
- `source_verification_agent` corpus-based cross-checking → v3.6.6+
- The `literature_corpus_consumers.md` second consumer block is a stub during the PR-A → PR-B window (see §6 Rollout).

### 2.5 Architectural risks

1. **Skeleton-vs-stub clarity.** The `literature_corpus_consumers.md` skeleton during PR-A must contain an explicit, lint-detectable stub marker for the academic-paper consumer block (`Status: Stub — implementation in PR-B (v3.6.5)`). Without this, the file could be misread as already covering both consumers.
2. **Prose-template drift.** The PRE-SCREENED block structure must be pinned via a markdown example block in agent.md (not described in prose only). LLMs improvising structure across model versions will drift; the lint enforces the example block exists.
3. **Backpointer drift.** The two agent backpointers point at one file; renaming or moving the reference doc requires updating both call sites. The lint enforces the backpointer string is present in each agent file listed in the manifest.

---

## 3. Data Flow

### 3.1 Shared corpus-first flow

`bibliography_agent` and `literature_strategist_agent` both follow the same five-step reading flow. Skill-specific deliverables (PRISMA documentation in deep-research; literature matrix and gap identification in academic-paper) are layered on top of the merged `final_included` set without altering the flow itself.

```
Step 0: Detect literature_corpus[] presence
    if passport has non-empty literature_corpus[]:
        → enter corpus-first flow
    else:
        → original external-DB-only flow (no behaviour change vs v3.6.4)
```

```
Step 1: Pre-screen corpus against current RQ
    for each entry in literature_corpus[]:
        a. read citation_key, title, authors, year, source_pointer
           (required fields per v3.6.4 schema)
        b. read optional venue, doi, tags, abstract, user_notes
           (use abstract / user_notes only if present;
           absence is normal — do NOT treat as a problem)
        c. apply current Inclusion / Exclusion criteria to
           title + abstract + tags
           (same criteria for adapter and external DB results)
        d. classify entry as:
             - INCLUDE  → add to pre_screened_included[]
             - EXCLUDE  → add to pre_screened_excluded[] with reason
             - SKIP     → add to pre_screened_skipped[] with reason
                          (only when entry is structurally readable
                          but semantically unusable — see §4 F1)
```

```
Step 2: Search-fills-gap (external DB)
    derive uncovered_topics =
      RQ subtopics − {topics covered by pre_screened_included[]}
    if uncovered_topics is empty AND user explicitly asked
       "use my corpus only":
        → skip external search, surface in PRE-SCREENED block
    else:
        → run original external DB search BUT scoped to uncovered_topics
        → external_included[] subject to the SAME Inclusion / Exclusion criteria
```

```
Step 3: Merge for downstream
    final_included = pre_screened_included[] ∪ external_included[]
    final_included feeds:
        - annotated bibliography (neutral, no source tag)
        - literature matrix (academic-paper only)
        - all subsequent Phase 1 deliverables
```

```
Step 4: Emit Search Strategy Report
    - PRE-SCREENED FROM USER CORPUS: block (see §3.2 template)
    - DATABASES: block (existing structure, unchanged)
    - INCLUSION / EXCLUSION CRITERIA: block (existing, unchanged)
    - RESULTS SUMMARY: ...
```

### 3.2 `PRE-SCREENED FROM USER CORPUS:` block template

This template is pinned in `literature_corpus_consumers.md` and reproduced verbatim in both consumer agent files. The lint enforces nine line markers (see §5 L7).

```markdown
PRE-SCREENED FROM USER CORPUS:
- Adapter: <obtained_via mode>           # e.g., zotero_export
- Snapshot date: <max(obtained_at)>       # ISO 8601
- Total entries scanned: <N>
- Pre-screening result:
  - Included: <K> entries
    citation_keys: [<k1>, <k2>, ...]
  - Excluded by inclusion / exclusion criteria: <E> entries
    citation_keys: [...]   (omit if 0)
  - Skipped (semantic problems, not schema): <S> entries
    citation_keys with reasons:
      - <key>: <reason>
    (omit this sub-block if 0)
- Note: presence in corpus does not imply inclusion;
  same criteria applied to corpus and external sources.
```

### 3.3 Out of data-flow scope (explicit)

- Agents do not recompute aggregate metadata. They take `obtained_via` mode and `obtained_at` max directly from entries.
- Agents do not write a rejection log. `rejection_log.yaml` is a v3.6.4 adapter-side artifact; consumer agents only read passport contents.
- Agents do not redact `abstract` or `user_notes`. Privacy is the v3.6.4 adapter contract's responsibility (PRIVATE FIELD annotation).

### 3.4 Data-flow risks

1. **Inclusion / Exclusion parity.** The lint cannot enforce that an LLM applies the same criteria to corpus and external sources. Agent files use BAD / GOOD example pairs (existing ARS anti-pattern teaching style) to make the parity rule load-bearing in prose.
2. **`uncovered_topics` derivation.** Decomposing an RQ into subtopics is LLM behaviour; the spec does not prescribe an algorithm. The spec only requires the invariant "external search must be scoped to uncovered topics, must not duplicate corpus inclusions". Detecting overlap between PRE-SCREENED `citation_keys` and DATABASES results is reserved for manual review and not enforced by lint.
3. **`citation_keys` list size.** For large corpora (500+ entries), the PRE-SCREENED block citation list grows. The spec does not cap this — reproducibility takes precedence over readability; users must see the full list to reproduce the report.

---

## 4. Failure Modes

### 4.1 Failure taxonomy

| ID | Failure | Trigger | Contract behaviour |
|---|---|---|---|
| **F1** | Individual entry semantically broken | source_pointer 404 on resolution attempt, abstract field is empty after privacy clearing, or the entry's content cannot be applied to the inclusion criteria | Skip entry, record in `pre_screened_skipped[]` with reason, list in PRE-SCREENED block |
| **F2** | Whole corpus schema-invalid | passport schema validation fails | STOP and emit `[CORPUS SCHEMA VIOLATION]` (v3.6.4 input-port violation) |
| **F3** | Corpus non-empty but zero hits | RQ does not intersect with corpus | Continue to external DB; PRE-SCREENED block forces zero-hit surface with three plausible causes |
| **F4** | `obtained_via` field missing or inconsistent | Some / all entries lack `obtained_via`, or multiple `obtained_via` values mixed | Surface mixed adapter origin in PRE-SCREENED block (does not block flow) |
| **F5** | Empty corpus (`literature_corpus: []`) vs missing field | Explicit empty array vs absent field | Treat both identically: presence-based detection → original external-DB-only flow |

### 4.2 F4 detail

`obtained_via` is optional and "strongly recommended" in v3.6.4 schema. Three sub-cases:

- **F4a — All entries lack `obtained_via`.** PRE-SCREENED block writes `Adapter: <unspecified>`, plus a trailing note: `Adapter origin not declared by entries; user-written adapter should populate obtained_via per v3.6.4 schema recommendation.`
- **F4b — Partial coverage.** Write `Adapter: <mode> (N of M entries declared)`, surfacing the ratio.
- **F4c — Mixed values.** Write `Adapter: mixed (zotero_export: K, obsidian_vault: L, ...)`, listing each origin with count.

Rationale: `obtained_via` is the reproducibility anchor, but its optional status means the spec cannot mandate presence; F4a/b/c make absence and inconsistency auditable rather than silent.

### 4.3 F5 detail

`literature_corpus: []` and an absent `literature_corpus` field are equivalent under the v3.6.4 schema (optional field, empty array allowed). v3.6.5 treats both identically as "no corpus to consume" — neither triggers the corpus-first flow, neither triggers F3 zero-hit surfacing. Rationale: a user with an adapter writing an empty array (KB temporarily has no entries matching the entry shape) should not see a misleading zero-hit warning.

### 4.4 Iron Rules

Four hard rules go into `literature_corpus_consumers.md` and both consumer agent files. Pattern follows v3.4.0 compliance agent's iron-rule writing style and harness-retirement debt-pattern §2 (silent-failure domain keeps negative framing as load-bearing).

1. **Iron Rule 1 — Same criteria.** "Apply the same Inclusion / Exclusion criteria to corpus entries and external database results. No exceptions."
2. **Iron Rule 2 — No silent skip.** "Any skipped corpus entry must be recorded in the PRE-SCREENED block's skipped sub-section with a reason. Silently dropping an entry is a prompt-layer violation."
3. **Iron Rule 3 — No corpus mutation.** "Consumer agents never modify, backfill, or derive new content into `literature_corpus[]`. Read only."
4. **Iron Rule 4 — Schema-violation halt.** "If a passport reaches a Phase 1 agent with `literature_corpus[]` failing schema validation, STOP and emit `[CORPUS SCHEMA VIOLATION]`. Do not attempt partial recovery."

Rule selection rationale:

- Iron Rule 1 enforces A1 corpus-first parity (corpus presence ≠ automatic inclusion).
- Iron Rule 2 enforces C-honest transparency.
- Iron Rule 3 enforces v3.6.4 input-port boundary.
- Iron Rule 4 enforces v3.6.4 lint as single source of truth — graceful recovery would mask broken upstream lint pipelines.

### 4.5 Fail-loud vs ARS graceful philosophy

ARS generally prefers graceful degradation (e.g., v3.4.0 compliance agent's mode-aware tier-based block). Iron Rule 4 deliberately fails loud. The spec records the rationale next to the rule so future reviewers do not soften it under "make it more graceful" pressure: F2 fail-loud is a signal that v3.6.4 input-port lint failed to run; making this graceful would let CI pipeline failures get buried.

### 4.6 Failure-mode risks

1. **F1 vs F4 ambiguity.** An entry without `obtained_via` could be classified as either F4 (statistical bookkeeping) or F1 (skip individual entry). Decision: F4 takes precedence — `obtained_via` absence is a statistical concern about the corpus as a whole, not a per-entry content concern. F1 is reserved for entries with content-level problems.
2. **Iron Rule 2 cross-LLM drift.** The lint enforces structural markers (PRE-SCREENED block + skipped sub-section template) but cannot verify the LLM actually records every skipped entry. Defended by BAD / GOOD example pairs in agent prose plus runtime observation in the first real SR run after ship. The `feedback_iterative_codex_audit_during_execution.md` per-gate codex audit pattern is available as an implementation-time defence (not part of the spec).
3. **F2 vs ARS graceful tension.** Documented in §4.5 immediately adjacent to Iron Rule 4 to prevent future soften-the-rule pressure.

---

## 5. Testing & Lint

### 5.1 Testing philosophy

H1 lint-only is the chosen testing strategy. The spec deliberately excludes deterministic behaviour tests, prose-match fixtures, and cross-model probes from the v3.6.5 ship gate.

- The lint enforces **prompt-layer structural invariants** — agent files contain the right guidance, the right structural markers, and the right backpointers.
- LLM behaviour validation is reserved for real systematic-review runs after ship and for ad-hoc per-artifact codex review during implementation. Neither is a CI gate.
- This treatment matches v3.5.1 reading-check probe (also lint-only) and avoids the prose-fixture brittleness that made earlier prompt-layer tests collapse on minor wording changes.

### 5.2 Nine lint invariants

`scripts/check_corpus_consumer_protocol.py` enforces:

| ID | Invariant | Check method |
|---|---|---|
| **L1** | `academic-pipeline/references/literature_corpus_consumers.md` exists | file exists |
| **L2** | Reference doc contains both consumer blocks; each block carries support status (PR-A: academic-paper block is a Stub) | markdown headings `## Consumer: bibliography_agent` and `## Consumer: literature_strategist_agent` both present; stub block contains exact string `**Status:** Stub — implementation in PR-B (v3.6.5)` |
| **L3** | Each agent.md listed in the manifest carries a backpointer to the consumer protocol doc | grep `academic-pipeline/references/literature_corpus_consumers.md` against each manifest entry's `agent_path` |
| **L4** | Each manifested agent.md contains the PRE-SCREENED block markdown template | grep template start `PRE-SCREENED FROM USER CORPUS:` inside a fenced markdown block |
| **L5** | Each manifested agent.md contains all four Iron Rule headings | grep four iron-rule titles (Same criteria / No silent skip / No corpus mutation / Schema-violation halt) |
| **L6** | Each manifested agent.md contains the corpus-first flow Step 0 – Step 4 description | grep five step headings (presence detection / pre-screen / search-fills-gap / merge / emit report) |
| **L7** | PRE-SCREENED block template contains all nine required line markers (Adapter / Snapshot date / Total entries scanned / Pre-screening result / Included / Excluded / Skipped / citation_keys / Note about same criteria) | grep nine line markers inside the fenced block |
| **L8** | `handoff_schemas.md` retired the deferred caveat | grep negative for `Consumer-side integration deferred to v3.6.5+`; grep positive for backpointer to `literature_corpus_consumers.md` |
| **L9** | Reference doc contains a BAD / GOOD example pair illustrating Iron Rule 2 | grep `<!-- BAD -->` and `<!-- GOOD -->` markers inside fenced blocks |

### 5.3 Lint implementation pattern

Pattern follows v3.6.4 commit `2b81117` (`check_literature_corpus_schema.py`) and v3.4.0 commit `42575ff` (`check_prisma_trAIce_freshness.py`).

- Pure Python, no external dependencies for grep-style checks (jsonschema only for the manifest validation).
- Exit 0 = pass, exit 1 = fail.
- One function per invariant; the main loop runs all checks and aggregates a fail list before printing.
- TDD red-green: each invariant has at least one positive test and one negative test (mirrors v3.4.0 commit `ee91335`'s 13 failing-tests-first pattern).
- Estimated unit tests: 9 invariants × (1 positive + 1 negative) = 18 tests, plus one integration test that runs the full lint against the actual v3.6.5 files. Approximately 20 tests total.

### 5.4 CI wiring

`feedback_codex_review_per_schema_increment.md` recorded the v3.4.0 PRISMA-trAIce freshness anti-pattern of writing a lint but forgetting to wire CI. v3.6.5 must wire:

```yaml
- name: Check corpus consumer protocol
  run: python scripts/check_corpus_consumer_protocol.py
```

Placement: after the `check_literature_corpus_schema` step in `.github/workflows/spec-consistency.yml` (same artifact family, ordered input → consumer).

### 5.5 PR-A vs PR-B lint partition (manifest-driven)

The challenge: PR-A ships when only `bibliography_agent` is fully implemented, but the reference doc must already mention both consumers (so PR-B can append cleanly). The lint must pass during PR-A without falsely claiming `literature_strategist_agent` is supported.

Solution: a manifest file `scripts/corpus_consumer_manifest.json` lists currently-supported consumers. L3, L4, L5, L6 iterate over manifest entries only. L2 verifies both consumer blocks exist in the reference doc, but the academic-paper block in PR-A carries the stub marker that the lint accepts.

```json
{
  "supported_consumers": [
    {
      "agent_path": "deep-research/agents/bibliography_agent.md",
      "skill": "deep-research",
      "since_version": "v3.6.5",
      "phase": "Phase 1"
    }
  ]
}
```

PR-B appends the second entry. Two alternative approaches were rejected:

- **Prose-marker-only opt-in (no manifest):** rejected — semantic prose markers as lint inputs are fragile (per `feedback_codex_review_per_schema_increment` cascade history).
- **Disable L3 in PR-A, enable in PR-B:** rejected — violates the H1 "ship-equals-enforce" premise; lint should never be half-enabled at ship time.

### 5.6 Out of lint scope (explicit)

- Whether the LLM actually executes the corpus-first flow at runtime.
- Whether the PRE-SCREENED block appears in real generated reports.
- Whether `citation_keys` listed correspond to RQ-relevant content.
- Whether external DB results actually avoid overlap with corpus inclusions.

All four are LLM behaviour-layer concerns. Validation is deferred to runtime observation in real SR runs.

### 5.7 Testing risks

1. **Manifest sync drift.** The manifest and the reference doc consumer blocks must stay aligned. The lint adds a bidirectional cross-check: every manifest entry must have a corresponding reference-doc consumer block, and vice versa. L2 absorbs this as a bidirectional invariant.
2. **L9 BAD / GOOD example fragility.** Codex review historically catches inconsistencies in author-written BAD examples (R3 deep-semantic-hole pattern, v3.6.3). Implementation should expect a codex round-2 review to catch L9 example issues; plan a buffer round.
3. **PR-B lint changes.** Adding the second manifest entry plus per-agent specialisations in L4 / L7 / L9 means PR-B touches the lint, not just agent.md. The PR-B implementation plan must list "lint changes" as an explicit task.

---

## 6. Rollout & Version Sweep

### 6.1 PR-A — `feat/v3.6.5-corpus-consumer-deep-research`

- **Base:** `main` at v3.6.4 (`aa96fa6`)
- **Scope:**
  - Add `academic-pipeline/references/literature_corpus_consumers.md` with the multi-consumer skeleton. The `bibliography_agent` block carries full content; the `literature_strategist_agent` block is a stub with `Status: Stub — implementation in PR-B (v3.6.5)` and a TODO list.
  - Add `scripts/corpus_consumer_manifest.json` containing only the `bibliography_agent` entry.
  - Add `scripts/check_corpus_consumer_protocol.py` and tests. All nine invariants enabled; L3 / L4 / L5 / L6 iterate manifest entries.
  - Modify `deep-research/agents/bibliography_agent.md`: add corpus-first flow, four Iron Rules, PRE-SCREENED template, backpointer.
  - Modify `shared/handoff_schemas.md` Schema 9 paragraph: retire deferred caveat, replace with backpointer to consumer doc.
  - Modify `.github/workflows/spec-consistency.yml`: add lint step.
  - Apply the v3.6.5 version sweep (seven touchpoints, see §6.3).
  - Add `CHANGELOG.md` `[3.6.5]` entry.
- **Untouched:** `academic-paper/agents/literature_strategist_agent.md` (PR-B scope).
- **Tag policy:** PR-A merge does **not** tag. The v3.6.5 tag is created at PR-B merge commit (see §6.4).

### 6.2 PR-B — `feat/v3.6.5-corpus-consumer-academic-paper`

- **Base:** `main` after PR-A merge (rebase as needed).
- **Scope:**
  - Modify `academic-paper/agents/literature_strategist_agent.md`: apply the same corpus-first flow, four Iron Rules, PRE-SCREENED template, backpointer. The literature-matrix and gap-identification sections gain corpus-aware additions.
  - Update `academic-pipeline/references/literature_corpus_consumers.md`: replace the `literature_strategist_agent` stub block with full content.
  - Update `scripts/corpus_consumer_manifest.json`: append the `literature_strategist_agent` entry.
  - Update `scripts/check_corpus_consumer_protocol.py` if any per-agent specialisations are needed in L4 / L7 / L9.
  - No new version sweep — PR-A already applied v3.6.5 across the seven touchpoints.
  - Append a single line to the existing `[3.6.5]` CHANGELOG entry: `Add literature_strategist_agent corpus consumer`. Do not open a new entry.

### 6.3 Seven version-sweep touchpoints (PR-A only)

Per `feedback_version_bump_sweep_checklist.md`:

1. `academic-pipeline/SKILL.md` frontmatter `version` and footer Skill Info: `3.6.4 → 3.6.5`.
2. `deep-research/SKILL.md` frontmatter `version` and footer Skill Info: `2.9.x → 2.9.(x+1)` patch bump.
3. `academic-paper/SKILL.md` frontmatter `version` and footer Skill Info: `3.1.x → 3.1.(x+1)` patch bump.
4. `MODE_REGISTRY.md` last-updated → `v3.6.5`.
5. `.claude/CLAUDE.md` Skills Overview table — three skill versions aligned.
6. `README.md` and `README.zh-TW.md` — badge / heading / changelog entry (Chinese version uses full-width parentheses).
7. `scripts/check_spec_consistency.py` version pin sweep — keep v3.3.2 – v3.6.4 lines as regression guards; append v3.6.5 expected heading and lint pin.

**Critical:** v3.6.4 commit `d54bd78` recorded the lesson that `check_spec_consistency.py` cross-validates `CHANGELOG.md`. Version bumps must occur in a single atomic commit covering SKILL.md, CHANGELOG, and spec consistency lint together. Splitting the bump across commits causes intermediate CI red. The implementation plan must explicitly mark this commit boundary.

### 6.4 Tag and release narrative

- PR-A merge: no tag, no release.
- PR-B merge: tag `v3.6.5` annotated on the PR-B merge commit. GitHub release titled `v3.6.5 — Material Passport literature_corpus[] Consumer Integration`, with both PR links in release notes.
- Rationale: from a user perspective v3.6.5 = consumer integration. Tagging on PR-A would publish a half-complete release; release narrative trumps ship sequence.
- The implementation plan must explicitly document this unusual sequence (tag at PR-B merge, not PR-A) so release operations do not default to tagging on the first merge.

### 6.5 `CHANGELOG.md` `[3.6.5]` structure

```markdown
## [3.6.5] - 2026-04-DD

### Added
- Material Passport `literature_corpus[]` consumer integration in Phase 1
  (deep-research/bibliography_agent + academic-paper/literature_strategist_agent).
  Corpus-first, search-fills-gap flow with PRE-SCREENED reproducibility block.
- `academic-pipeline/references/literature_corpus_consumers.md` — consumer protocol
  reference with four Iron Rules and per-consumer reading instructions.
- `scripts/check_corpus_consumer_protocol.py` — CI lint enforcing nine protocol invariants.
- `scripts/corpus_consumer_manifest.json` — supported-consumer manifest.

### Changed
- `shared/handoff_schemas.md` Schema 9 — retired the v3.6.4 "Consumer-side integration
  deferred to v3.6.5+" caveat; replaced with backpointer to the consumer protocol.
- `deep-research/SKILL.md` 2.9.x → 2.9.(x+1) — bibliography_agent corpus-first flow.
- `academic-paper/SKILL.md` 3.1.x → 3.1.(x+1) — literature_strategist_agent corpus-first flow.
- `academic-pipeline/SKILL.md` 3.6.4 → 3.6.5 — suite version invariant.

### Notes
- Consumer integration is presence-based (auto-engages when passport carries
  non-empty `literature_corpus[]`). No new env flag introduced.
- Schema is unchanged from v3.6.4. Existing user adapters work without modification.
- `citation_compliance_agent` corpus integration deferred to v3.6.6+.
```

### 6.6 Pre-merge gate sequence (per PR)

1. Local: `python scripts/check_corpus_consumer_protocol.py` exit 0.
2. Local: `python scripts/check_spec_consistency.py` exit 0.
3. Local: `pytest scripts/` (adapter tests + new lint tests + existing suite) all green.
4. Push branch — CI `spec-consistency.yml` and `pytest.yml` all green.
5. Per-artifact codex review during construction: consumer protocol doc, lint script, agent.md change, version sweep — each receives an incremental review. This avoids the v3.6.4 PR #40 4566-line codex hang lesson by keeping each review's input bounded. Pattern from `feedback_codex_review_per_schema_increment.md`.
6. `/simplify` code review pass.
7. PR open with `--base main`.
8. Optional final codex review on full PR diff if the diff remains digestible. If the PR exceeds the v3.6.4 hang threshold, rely on per-artifact review history instead.

### 6.7 Rollout risks

1. **Stub-period exposure.** Between PR-A merge and PR-B merge, the reference doc carries a stub block, the manifest lists one consumer, and `literature_strategist_agent.md` has no backpointer. The stub block must explicitly state `Status: Stub — coming in PR-B (target: <date>)`. Spec mandates a maximum stub window of five working days; longer requires upgrading the stub note to `v3.6.5-pre delayed` and informing users.
2. **CHANGELOG rebase conflict.** PR-A finalises the `[3.6.5]` entry; PR-B appends one line. The implementation plan must document PR-B's rebase strategy (fetch main, rebase, append-conflict resolution).
3. **Tag ops default.** Tagging at PR-B is unusual. The implementation plan must include a release-ops checklist line: "Do not tag at PR-A merge. Tag at PR-B merge."
4. **PR-B blocked path.** If PR-B cannot ship within five working days, the fallback is to rename the release v3.6.5 → v3.6.5a, ship deep-research consumer only, and move `literature_strategist_agent` to v3.6.6. The spec records this fallback explicitly so it is not invented under pressure.

---

## 7. Post-Ship Follow-ups (Backlog Anchors)

These items are recorded for the project memory but are out of v3.6.5 scope:

- Real SR run observation: corpus-first flow drift, silent-skip rate, criteria-parity adherence under live LLM behaviour.
- v3.6.6+ candidates: `citation_compliance_agent` Phase 5a corpus reading; `source_verification_agent` corpus-based source cross-checking.
- Real-user adapter samples feeding back into v3.6.4 reference adapter expansions.

---

## 8. Decision Log Summary

| Q | Topic | Decision |
|---|---|---|
| Q1 | Consumer scope | A — `bibliography_agent` + `literature_strategist_agent`; `citation_compliance_agent` not in v3.6.5 |
| Q2 | Phase 1 flow | A1 — Corpus-first, search-fills-gap |
| Q3 | Opt-in mechanism | B1 — Presence-based auto-read |
| Q4 | Failure mode philosophy | C-honest — Skip individual entries but surface to user |
| Q5 | PRISMA-style record | D2 — Independent `PRE-SCREENED FROM USER CORPUS:` block |
| Q6 | Annotated bib attribution | E2 — Bibliography stays neutral; attribution lives in search documentation |
| Q7 | Schema impact | F1 — No schema change; retire deferred caveats only |
| Q8 | Reference doc location | G1 — `academic-pipeline/references/literature_corpus_consumers.md` |
| Q9 | Testing strategy | H1 — Lint-only, nine invariants, CI wired |
| Q10 | Version bump | I1 — Suite v3.6.5 patch |
| Q11 | Implementation order | Path 3 — Two PRs, one v3.6.5 tag at PR-B |
| Q12 | Which PR first | PR-A deep-research first; PR-B academic-paper second |

---
