fixture_id: omitted
schema_field: output_language_pair
contract: shared/output_language_pair.md
cases:
  - name: field omitted (legacy run)
    expected_valid: true
    expected_status: legacy
    error_contains: []
notes: |
  Absence is the legacy state, not a gap: a handoff with no `output_language_pair` key
  keeps exactly today's zh-TW + EN bilingual output. The legacy object keys
  (`abstract: {english, chinese}`, `keywords: {en, zh_tw}`) are reproduced unchanged.
